
/**
 * @description The last action performed on the rolling upgrade.
 */
export enum RollingUpgradeActionType {
    /**
     *
     */
    Start = 'Start',
    /**
     *
     */
    Cancel = 'Cancel'
}
