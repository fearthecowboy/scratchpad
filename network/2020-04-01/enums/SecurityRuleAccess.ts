
/**
 * @extensible
 * @description Whether network traffic is allowed or denied.
 */
export enum SecurityRuleAccess {
    /**
     *
     */
    Allow = 'Allow',
    /**
     *
     */
    Deny = 'Deny'
}
