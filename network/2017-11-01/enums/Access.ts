
/**
 * @extensible
 * @description The access type of the rule. Valid values are: 'Allow', 'Deny'
 * @todo temporary-reuse-marker
 */
export enum Access {
    /**
     *
     */
    Allow = 'Allow',
    /**
     *
     */
    Deny = 'Deny'
}
